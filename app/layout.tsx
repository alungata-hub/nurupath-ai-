import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { AuthProvider } from '@/lib/auth-context';
import { Toaster } from '@/components/ui/sonner';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NuruPath AI - Career Navigation for Kenya',
  description: 'AI-powered career navigation platform helping Kenyan youth identify education-to-employment pathways.',
  openGraph: {
    title: 'NuruPath AI',
    description: 'AI-powered career navigation for Kenya',
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
          <Toaster />
        </AuthProvider>
      </body>
    </html>
  );
}
